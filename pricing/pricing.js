 /*function toggleView() {
    // get the info
    var moreInfo = document.getElementById('info');

    // get the current value of the info's display property
    var displaySetting = moreInfo.style.display;

    // also get the info button, so we can change what it says
    var infoButton = document.getElementById('infoButton');

    // toggle the info and the button text, depending on current state
    if (displaySetting == 'block') {
      // info is visible; hide it
      moreInfo.style.display = 'none';
      // change button text
      infoButton.innerHTML = 'More Info';
    }
    else {
      // info is hidden. show it
      moreInfo.style.display = 'block';
      // change button text
      infoButton.innerHTML = 'Hide Info';
    }
  }*/